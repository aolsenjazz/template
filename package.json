{
  "name": "monorepo-starter",
  "private": true,
  "scripts": {
    "tsc": "pnpm -r tsc",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "build": "pnpm --filter @app/web build"
  }
}
